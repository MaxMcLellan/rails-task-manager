<ul>
  <% @tasks.each do |task| %>
    <li><%= task.title %></li>
  <% end %>
</ul>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<%# <%= form_for @tasks do |f| %>
  <%# <%= f.text_field :title %>
  <%# <%= f.text_field :details %>
  <%# <%= f.check_box :completed %>
<%# <% end %>
